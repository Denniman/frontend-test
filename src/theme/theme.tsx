import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
    fonts: {
        heading: `'CircularStd-Bold', sans-serif`,
        body: `'CircularStd-Medium"', sans-serif`,
    },
});
